// Static array: Fixed in size (need to specify number of elements ahead of time) => C++
// Dynamic array: Allow copying and rebuilding array in a new location with more memory available for new items
// In JS arrays are automatically dynamic BUT arrays are being copied so when adding items if no mem is available the Big O becomes O(n) instead of O(1)

// C++
// int a[20]
// int b[5] {1, 2, 3, 4, 5}